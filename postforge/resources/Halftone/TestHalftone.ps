%!PS-Adobe-3.0 Resource-Halftone
%%Title: TestHalftone (Stub)
%%Creator: PostForge Resource Management System
%%VMusage: 2000 30
%%EndComments

% This is a stub halftone resource for testing PostForge resource management  
% A real halftone would contain actual halftoning parameters

<<
    /HalftoneType 1  % Spot function halftone
    
    /Frequency 60    % Lines per inch
    /Angle 45        % Screen angle in degrees
    /SpotFunction { % Spot function procedure
        abs exch abs 2 copy add 1 le {
            dup mul exch dup mul add 1 exch sub
        } {
            2 copy add 1 sub dup mul
            exch 1 sub dup mul add 1 sub
        } ifelse
    } bind
    
    /AccurateScreens true
    /Transfer { } bind  % Identity transfer function
    
>> % Halftone dictionary completed

(TestHalftone resource loaded\n) print